table:
  name: idp
  schema: public
is_enum: true
array_relationships:
- name: identities
  using:
    foreign_key_constraint_on:
      column: idp
      table:
        name: identity
        schema: public
